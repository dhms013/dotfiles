return {
  "neo-tree.nvim",
  opts = {
    filesystem = {
      filtered_items = {
        hide_dotfiles = false, -- Set to false to always show hidden files
        -- You can also adjust other filtering options here if needed
        -- hide_gitignored = true,
        -- hide_by_name = {
        --   ".github",
        --   ".gitignore",
        -- },
      },
    },
  },
}
